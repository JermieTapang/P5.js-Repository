let trail = [];
let maxTrailLength = 20;

var d = 50;

function setup() {
  createCanvas(800, 600);
  background(0)
  noStroke()
  mouseX = -d
  movedY = -d
}

function draw() {

  fill(255,50,10)
  ellipse(mouseX, mouseY, 50,50)
  
  
}